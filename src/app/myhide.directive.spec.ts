import { MyhideDirective } from './myhide.directive';

describe('MyhideDirective', () => {
  it('should create an instance', () => {
    const directive = new MyhideDirective();
    expect(directive).toBeTruthy();
  });
});
